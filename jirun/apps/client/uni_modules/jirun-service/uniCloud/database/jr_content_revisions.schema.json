{
  "bsonType": "object",
  "required": ["contentId", "revision", "reviewStatus", "createdAt"],
  "permission": {
    "read": false,
    "create": false,
    "update": false,
    "delete": false
  },
  "properties": {
    "_id": {
      "description": "版本 ID，由云端生成"
    },
    "contentId": {
      "bsonType": "string",
      "description": "所属根内容 ID"
    },
    "revision": {
      "bsonType": "int",
      "description": "版本号，从 1 开始递增；编辑一次产生一个新版本"
    },
    "title": {
      "bsonType": "string",
      "description": "标题，最多 60 个 Unicode 码点；校园墙可为空"
    },
    "body": {
      "bsonType": "string",
      "description": "正文，最多 3000 个 Unicode 码点"
    },
    "mediaIds": {
      "bsonType": "array",
      "description": "引用的媒体 ID 列表（jr_media._id），最多 9 张图片或 1 段视频"
    },
    "details": {
      "bsonType": "object",
      "description": "按类型校验的明细字段（取送/闲置/求购/校园墙各自必需项不同）"
    },
    "reviewStatus": {
      "bsonType": "string",
      "description": "该版本的审核状态：只有 approved 的版本可以被公开查询返回",
      "enum": ["pending", "approved", "rejected"]
    },
    "createdAt": {
      "bsonType": "long",
      "description": "版本创建时间，服务端生成的 Unix 毫秒"
    }
  }
}
